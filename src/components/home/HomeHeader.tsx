import { ReactNode } from 'react';
export function getGreeting(hour: number): string { if (hour < 5) return 'Доброй ночи'; if (hour < 12) return 'Доброе утро'; if (hour < 17) return 'Добрый день'; if (hour < 22) return 'Добрый вечер'; return 'Доброй ночи'; }
export function HomeHeader({ now, status }: { now: number; status: ReactNode }) { return <header className="flex items-start justify-between gap-4"><div><p className="font-display text-title-xl font-extrabold tracking-[.12em]">USNEE</p><h1 className="mt-3 text-title-lg">{getGreeting(new Date(now).getHours())}</h1><p className="mt-1 text-body-sm text-usnee-text2">Данные доступны локально и без сети</p></div>{status}</header>; }
